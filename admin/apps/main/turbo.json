{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "src/**/*.ts",
        "src/**/*.tsx",
        "Dockerfile",
        "nodemon.json",
        "package.json",
        "tsconfig*.json"
      ],
      "outputs": ["dist/**", "build/**"]
    }
  }
}
