{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "src/**/*.ts",
        "!src/**/*.spec.ts",
        "package.json",
        "tsconfig*.json",
        "tsdown.config.mts"
      ],
      "outputs": ["dist/**"]
    }
  }
}
