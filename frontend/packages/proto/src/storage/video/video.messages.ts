// @ts-nocheck
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.35.1
// source: storage/video/video.messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from '@bufbuild/protobuf/wire';
import { StorageManyMeta, StorageMeta } from '../common/common.messages';
import { FileCreate } from '../file/file.messages';
import { Video } from './video';
import { VideoPopulated } from './video.populates';

export interface VideoQuery {
  id?: string;
  ids: string[];
  file?: string;
  providerId?: string;
  providerIds: string[];
  userId?: string;
}

export interface VideoList {
  items: VideoPopulated[];
  total: number;
}

export interface VideoCreate {
  title: string;
  description?: string;
}

export interface VideoCreateOne {
  video: VideoCreate;
  file: FileCreate;
  storage?: StorageMeta;
  userId: string;
}

export interface VideoCreateOneWeb {
  video: VideoCreate;
  file: FileCreate;
  storage?: StorageMeta;
}

export interface VideoCreateManyItem {
  video: VideoCreate;
  file: FileCreate;
  uploadId: string;
}

export interface VideoCreateMany {
  storage?: StorageManyMeta;
  items: VideoCreateManyItem[];
  userId: string;
}

export interface VideoCreateManyWeb {
  storage?: StorageManyMeta;
  items: VideoCreateManyItem[];
}

/**
 * Pre-signed credentials for a direct browser -> Bunny Stream TUS upload.
 * `expires` and `signature` are carried as strings because Bunny rejects the upload
 * unless the AuthorizationExpire header matches the value that was signed byte-for-byte.
 */
export interface VideoTusUpload {
  endpoint: string;
  libraryId: string;
  videoId: string;
  signature: string;
  expires: string;
  filetype: string;
  title: string;
}

export interface VideoCreated {
  video: Video;
  upload: VideoTusUpload;
}

export interface VideoCreatedArray {
  items: VideoCreated[];
}

export interface VideoUpdateSet {
  title?: string;
  description?: string;
}

export interface VideoUpdate {
  set?: VideoUpdateSet;
}

export interface VideoUpdateOne {
  query: VideoQuery;
  update: VideoUpdate;
}

export interface VideoUpdateById {
  id: string;
  update: VideoUpdate;
}

function createBaseVideoQuery(): VideoQuery {
  return {
    id: undefined,
    ids: [],
    file: undefined,
    providerId: undefined,
    providerIds: [],
    userId: undefined,
  };
}

export const VideoQuery: MessageFns<VideoQuery> = {
  encode(message: VideoQuery, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      writer.uint32(10).string(message.id);
    }
    for (const v of message.ids) {
      writer.uint32(18).string(v!);
    }
    if (message.file !== undefined) {
      writer.uint32(26).string(message.file);
    }
    if (message.providerId !== undefined) {
      writer.uint32(34).string(message.providerId);
    }
    for (const v of message.providerIds) {
      writer.uint32(42).string(v!);
    }
    if (message.userId !== undefined) {
      writer.uint32(50).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoQuery {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoQuery();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ids.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.file = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.providerId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.providerIds.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoQuery {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : undefined,
      ids: globalThis.Array.isArray(object?.ids)
        ? object.ids.map((e: any) => globalThis.String(e))
        : [],
      file: isSet(object.file) ? globalThis.String(object.file) : undefined,
      providerId: isSet(object.providerId) ? globalThis.String(object.providerId) : undefined,
      providerIds: globalThis.Array.isArray(object?.providerIds)
        ? object.providerIds.map((e: any) => globalThis.String(e))
        : [],
      userId: isSet(object.userId) ? globalThis.String(object.userId) : undefined,
    };
  },

  toJSON(message: VideoQuery): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = message.id;
    }
    if (message.ids?.length) {
      obj.ids = message.ids;
    }
    if (message.file !== undefined) {
      obj.file = message.file;
    }
    if (message.providerId !== undefined) {
      obj.providerId = message.providerId;
    }
    if (message.providerIds?.length) {
      obj.providerIds = message.providerIds;
    }
    if (message.userId !== undefined) {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoQuery>, I>>(base?: I): VideoQuery {
    return VideoQuery.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoQuery>, I>>(object: I): VideoQuery {
    const message = createBaseVideoQuery();
    message.id = object.id ?? undefined;
    message.ids = object.ids?.map((e) => e) || [];
    message.file = object.file ?? undefined;
    message.providerId = object.providerId ?? undefined;
    message.providerIds = object.providerIds?.map((e) => e) || [];
    message.userId = object.userId ?? undefined;
    return message;
  },
};

function createBaseVideoList(): VideoList {
  return { items: [], total: 0 };
}

export const VideoList: MessageFns<VideoList> = {
  encode(message: VideoList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      VideoPopulated.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).int32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(VideoPopulated.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoList {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => VideoPopulated.fromJSON(e))
        : [],
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: VideoList): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => VideoPopulated.toJSON(e));
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoList>, I>>(base?: I): VideoList {
    return VideoList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoList>, I>>(object: I): VideoList {
    const message = createBaseVideoList();
    message.items = object.items?.map((e) => VideoPopulated.fromPartial(e)) || [];
    message.total = object.total ?? 0;
    return message;
  },
};

function createBaseVideoCreate(): VideoCreate {
  return { title: '', description: undefined };
}

export const VideoCreate: MessageFns<VideoCreate> = {
  encode(message: VideoCreate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== '') {
      writer.uint32(10).string(message.title);
    }
    if (message.description !== undefined) {
      writer.uint32(18).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreate {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : '',
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
    };
  },

  toJSON(message: VideoCreate): unknown {
    const obj: any = {};
    if (message.title !== '') {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreate>, I>>(base?: I): VideoCreate {
    return VideoCreate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreate>, I>>(object: I): VideoCreate {
    const message = createBaseVideoCreate();
    message.title = object.title ?? '';
    message.description = object.description ?? undefined;
    return message;
  },
};

function createBaseVideoCreateOne(): VideoCreateOne {
  return { video: undefined, file: undefined, storage: undefined, userId: '' };
}

export const VideoCreateOne: MessageFns<VideoCreateOne> = {
  encode(message: VideoCreateOne, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.video !== undefined) {
      VideoCreate.encode(message.video, writer.uint32(10).fork()).join();
    }
    if (message.file !== undefined) {
      FileCreate.encode(message.file, writer.uint32(18).fork()).join();
    }
    if (message.storage !== undefined) {
      StorageMeta.encode(message.storage, writer.uint32(26).fork()).join();
    }
    if (message.userId !== '') {
      writer.uint32(34).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreateOne {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreateOne();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.video = VideoCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = FileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.storage = StorageMeta.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreateOne {
    return {
      video: isSet(object.video) ? VideoCreate.fromJSON(object.video) : undefined,
      file: isSet(object.file) ? FileCreate.fromJSON(object.file) : undefined,
      storage: isSet(object.storage) ? StorageMeta.fromJSON(object.storage) : undefined,
      userId: isSet(object.userId) ? globalThis.String(object.userId) : '',
    };
  },

  toJSON(message: VideoCreateOne): unknown {
    const obj: any = {};
    if (message.video !== undefined) {
      obj.video = VideoCreate.toJSON(message.video);
    }
    if (message.file !== undefined) {
      obj.file = FileCreate.toJSON(message.file);
    }
    if (message.storage !== undefined) {
      obj.storage = StorageMeta.toJSON(message.storage);
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreateOne>, I>>(base?: I): VideoCreateOne {
    return VideoCreateOne.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreateOne>, I>>(object: I): VideoCreateOne {
    const message = createBaseVideoCreateOne();
    message.video =
      object.video !== undefined && object.video !== null
        ? VideoCreate.fromPartial(object.video)
        : undefined;
    message.file =
      object.file !== undefined && object.file !== null
        ? FileCreate.fromPartial(object.file)
        : undefined;
    message.storage =
      object.storage !== undefined && object.storage !== null
        ? StorageMeta.fromPartial(object.storage)
        : undefined;
    message.userId = object.userId ?? '';
    return message;
  },
};

function createBaseVideoCreateOneWeb(): VideoCreateOneWeb {
  return { video: undefined, file: undefined, storage: undefined };
}

export const VideoCreateOneWeb: MessageFns<VideoCreateOneWeb> = {
  encode(message: VideoCreateOneWeb, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.video !== undefined) {
      VideoCreate.encode(message.video, writer.uint32(10).fork()).join();
    }
    if (message.file !== undefined) {
      FileCreate.encode(message.file, writer.uint32(18).fork()).join();
    }
    if (message.storage !== undefined) {
      StorageMeta.encode(message.storage, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreateOneWeb {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreateOneWeb();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.video = VideoCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = FileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.storage = StorageMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreateOneWeb {
    return {
      video: isSet(object.video) ? VideoCreate.fromJSON(object.video) : undefined,
      file: isSet(object.file) ? FileCreate.fromJSON(object.file) : undefined,
      storage: isSet(object.storage) ? StorageMeta.fromJSON(object.storage) : undefined,
    };
  },

  toJSON(message: VideoCreateOneWeb): unknown {
    const obj: any = {};
    if (message.video !== undefined) {
      obj.video = VideoCreate.toJSON(message.video);
    }
    if (message.file !== undefined) {
      obj.file = FileCreate.toJSON(message.file);
    }
    if (message.storage !== undefined) {
      obj.storage = StorageMeta.toJSON(message.storage);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreateOneWeb>, I>>(base?: I): VideoCreateOneWeb {
    return VideoCreateOneWeb.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreateOneWeb>, I>>(object: I): VideoCreateOneWeb {
    const message = createBaseVideoCreateOneWeb();
    message.video =
      object.video !== undefined && object.video !== null
        ? VideoCreate.fromPartial(object.video)
        : undefined;
    message.file =
      object.file !== undefined && object.file !== null
        ? FileCreate.fromPartial(object.file)
        : undefined;
    message.storage =
      object.storage !== undefined && object.storage !== null
        ? StorageMeta.fromPartial(object.storage)
        : undefined;
    return message;
  },
};

function createBaseVideoCreateManyItem(): VideoCreateManyItem {
  return { video: undefined, file: undefined, uploadId: '' };
}

export const VideoCreateManyItem: MessageFns<VideoCreateManyItem> = {
  encode(message: VideoCreateManyItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.video !== undefined) {
      VideoCreate.encode(message.video, writer.uint32(10).fork()).join();
    }
    if (message.file !== undefined) {
      FileCreate.encode(message.file, writer.uint32(18).fork()).join();
    }
    if (message.uploadId !== '') {
      writer.uint32(26).string(message.uploadId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreateManyItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreateManyItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.video = VideoCreate.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.file = FileCreate.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreateManyItem {
    return {
      video: isSet(object.video) ? VideoCreate.fromJSON(object.video) : undefined,
      file: isSet(object.file) ? FileCreate.fromJSON(object.file) : undefined,
      uploadId: isSet(object.uploadId) ? globalThis.String(object.uploadId) : '',
    };
  },

  toJSON(message: VideoCreateManyItem): unknown {
    const obj: any = {};
    if (message.video !== undefined) {
      obj.video = VideoCreate.toJSON(message.video);
    }
    if (message.file !== undefined) {
      obj.file = FileCreate.toJSON(message.file);
    }
    if (message.uploadId !== '') {
      obj.uploadId = message.uploadId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreateManyItem>, I>>(base?: I): VideoCreateManyItem {
    return VideoCreateManyItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreateManyItem>, I>>(
    object: I,
  ): VideoCreateManyItem {
    const message = createBaseVideoCreateManyItem();
    message.video =
      object.video !== undefined && object.video !== null
        ? VideoCreate.fromPartial(object.video)
        : undefined;
    message.file =
      object.file !== undefined && object.file !== null
        ? FileCreate.fromPartial(object.file)
        : undefined;
    message.uploadId = object.uploadId ?? '';
    return message;
  },
};

function createBaseVideoCreateMany(): VideoCreateMany {
  return { storage: undefined, items: [], userId: '' };
}

export const VideoCreateMany: MessageFns<VideoCreateMany> = {
  encode(message: VideoCreateMany, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.storage !== undefined) {
      StorageManyMeta.encode(message.storage, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      VideoCreateManyItem.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.userId !== '') {
      writer.uint32(26).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreateMany {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreateMany();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storage = StorageManyMeta.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(VideoCreateManyItem.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreateMany {
    return {
      storage: isSet(object.storage) ? StorageManyMeta.fromJSON(object.storage) : undefined,
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => VideoCreateManyItem.fromJSON(e))
        : [],
      userId: isSet(object.userId) ? globalThis.String(object.userId) : '',
    };
  },

  toJSON(message: VideoCreateMany): unknown {
    const obj: any = {};
    if (message.storage !== undefined) {
      obj.storage = StorageManyMeta.toJSON(message.storage);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => VideoCreateManyItem.toJSON(e));
    }
    if (message.userId !== '') {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreateMany>, I>>(base?: I): VideoCreateMany {
    return VideoCreateMany.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreateMany>, I>>(object: I): VideoCreateMany {
    const message = createBaseVideoCreateMany();
    message.storage =
      object.storage !== undefined && object.storage !== null
        ? StorageManyMeta.fromPartial(object.storage)
        : undefined;
    message.items = object.items?.map((e) => VideoCreateManyItem.fromPartial(e)) || [];
    message.userId = object.userId ?? '';
    return message;
  },
};

function createBaseVideoCreateManyWeb(): VideoCreateManyWeb {
  return { storage: undefined, items: [] };
}

export const VideoCreateManyWeb: MessageFns<VideoCreateManyWeb> = {
  encode(message: VideoCreateManyWeb, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.storage !== undefined) {
      StorageManyMeta.encode(message.storage, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      VideoCreateManyItem.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreateManyWeb {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreateManyWeb();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.storage = StorageManyMeta.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(VideoCreateManyItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreateManyWeb {
    return {
      storage: isSet(object.storage) ? StorageManyMeta.fromJSON(object.storage) : undefined,
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => VideoCreateManyItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: VideoCreateManyWeb): unknown {
    const obj: any = {};
    if (message.storage !== undefined) {
      obj.storage = StorageManyMeta.toJSON(message.storage);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => VideoCreateManyItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreateManyWeb>, I>>(base?: I): VideoCreateManyWeb {
    return VideoCreateManyWeb.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreateManyWeb>, I>>(object: I): VideoCreateManyWeb {
    const message = createBaseVideoCreateManyWeb();
    message.storage =
      object.storage !== undefined && object.storage !== null
        ? StorageManyMeta.fromPartial(object.storage)
        : undefined;
    message.items = object.items?.map((e) => VideoCreateManyItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVideoTusUpload(): VideoTusUpload {
  return {
    endpoint: '',
    libraryId: '',
    videoId: '',
    signature: '',
    expires: '',
    filetype: '',
    title: '',
  };
}

export const VideoTusUpload: MessageFns<VideoTusUpload> = {
  encode(message: VideoTusUpload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.endpoint !== '') {
      writer.uint32(10).string(message.endpoint);
    }
    if (message.libraryId !== '') {
      writer.uint32(18).string(message.libraryId);
    }
    if (message.videoId !== '') {
      writer.uint32(26).string(message.videoId);
    }
    if (message.signature !== '') {
      writer.uint32(34).string(message.signature);
    }
    if (message.expires !== '') {
      writer.uint32(42).string(message.expires);
    }
    if (message.filetype !== '') {
      writer.uint32(50).string(message.filetype);
    }
    if (message.title !== '') {
      writer.uint32(58).string(message.title);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoTusUpload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoTusUpload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.endpoint = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.libraryId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.videoId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.signature = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.expires = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.filetype = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.title = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoTusUpload {
    return {
      endpoint: isSet(object.endpoint) ? globalThis.String(object.endpoint) : '',
      libraryId: isSet(object.libraryId) ? globalThis.String(object.libraryId) : '',
      videoId: isSet(object.videoId) ? globalThis.String(object.videoId) : '',
      signature: isSet(object.signature) ? globalThis.String(object.signature) : '',
      expires: isSet(object.expires) ? globalThis.String(object.expires) : '',
      filetype: isSet(object.filetype) ? globalThis.String(object.filetype) : '',
      title: isSet(object.title) ? globalThis.String(object.title) : '',
    };
  },

  toJSON(message: VideoTusUpload): unknown {
    const obj: any = {};
    if (message.endpoint !== '') {
      obj.endpoint = message.endpoint;
    }
    if (message.libraryId !== '') {
      obj.libraryId = message.libraryId;
    }
    if (message.videoId !== '') {
      obj.videoId = message.videoId;
    }
    if (message.signature !== '') {
      obj.signature = message.signature;
    }
    if (message.expires !== '') {
      obj.expires = message.expires;
    }
    if (message.filetype !== '') {
      obj.filetype = message.filetype;
    }
    if (message.title !== '') {
      obj.title = message.title;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoTusUpload>, I>>(base?: I): VideoTusUpload {
    return VideoTusUpload.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoTusUpload>, I>>(object: I): VideoTusUpload {
    const message = createBaseVideoTusUpload();
    message.endpoint = object.endpoint ?? '';
    message.libraryId = object.libraryId ?? '';
    message.videoId = object.videoId ?? '';
    message.signature = object.signature ?? '';
    message.expires = object.expires ?? '';
    message.filetype = object.filetype ?? '';
    message.title = object.title ?? '';
    return message;
  },
};

function createBaseVideoCreated(): VideoCreated {
  return { video: undefined, upload: undefined };
}

export const VideoCreated: MessageFns<VideoCreated> = {
  encode(message: VideoCreated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.video !== undefined) {
      Video.encode(message.video, writer.uint32(10).fork()).join();
    }
    if (message.upload !== undefined) {
      VideoTusUpload.encode(message.upload, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.video = Video.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.upload = VideoTusUpload.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreated {
    return {
      video: isSet(object.video) ? Video.fromJSON(object.video) : undefined,
      upload: isSet(object.upload) ? VideoTusUpload.fromJSON(object.upload) : undefined,
    };
  },

  toJSON(message: VideoCreated): unknown {
    const obj: any = {};
    if (message.video !== undefined) {
      obj.video = Video.toJSON(message.video);
    }
    if (message.upload !== undefined) {
      obj.upload = VideoTusUpload.toJSON(message.upload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreated>, I>>(base?: I): VideoCreated {
    return VideoCreated.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreated>, I>>(object: I): VideoCreated {
    const message = createBaseVideoCreated();
    message.video =
      object.video !== undefined && object.video !== null
        ? Video.fromPartial(object.video)
        : undefined;
    message.upload =
      object.upload !== undefined && object.upload !== null
        ? VideoTusUpload.fromPartial(object.upload)
        : undefined;
    return message;
  },
};

function createBaseVideoCreatedArray(): VideoCreatedArray {
  return { items: [] };
}

export const VideoCreatedArray: MessageFns<VideoCreatedArray> = {
  encode(message: VideoCreatedArray, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      VideoCreated.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoCreatedArray {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoCreatedArray();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(VideoCreated.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoCreatedArray {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => VideoCreated.fromJSON(e))
        : [],
    };
  },

  toJSON(message: VideoCreatedArray): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => VideoCreated.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoCreatedArray>, I>>(base?: I): VideoCreatedArray {
    return VideoCreatedArray.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoCreatedArray>, I>>(object: I): VideoCreatedArray {
    const message = createBaseVideoCreatedArray();
    message.items = object.items?.map((e) => VideoCreated.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVideoUpdateSet(): VideoUpdateSet {
  return { title: undefined, description: undefined };
}

export const VideoUpdateSet: MessageFns<VideoUpdateSet> = {
  encode(message: VideoUpdateSet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== undefined) {
      writer.uint32(10).string(message.title);
    }
    if (message.description !== undefined) {
      writer.uint32(18).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoUpdateSet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoUpdateSet();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoUpdateSet {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
    };
  },

  toJSON(message: VideoUpdateSet): unknown {
    const obj: any = {};
    if (message.title !== undefined) {
      obj.title = message.title;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoUpdateSet>, I>>(base?: I): VideoUpdateSet {
    return VideoUpdateSet.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoUpdateSet>, I>>(object: I): VideoUpdateSet {
    const message = createBaseVideoUpdateSet();
    message.title = object.title ?? undefined;
    message.description = object.description ?? undefined;
    return message;
  },
};

function createBaseVideoUpdate(): VideoUpdate {
  return { set: undefined };
}

export const VideoUpdate: MessageFns<VideoUpdate> = {
  encode(message: VideoUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.set !== undefined) {
      VideoUpdateSet.encode(message.set, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.set = VideoUpdateSet.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoUpdate {
    return { set: isSet(object.set) ? VideoUpdateSet.fromJSON(object.set) : undefined };
  },

  toJSON(message: VideoUpdate): unknown {
    const obj: any = {};
    if (message.set !== undefined) {
      obj.set = VideoUpdateSet.toJSON(message.set);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoUpdate>, I>>(base?: I): VideoUpdate {
    return VideoUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoUpdate>, I>>(object: I): VideoUpdate {
    const message = createBaseVideoUpdate();
    message.set =
      object.set !== undefined && object.set !== null
        ? VideoUpdateSet.fromPartial(object.set)
        : undefined;
    return message;
  },
};

function createBaseVideoUpdateOne(): VideoUpdateOne {
  return { query: undefined, update: undefined };
}

export const VideoUpdateOne: MessageFns<VideoUpdateOne> = {
  encode(message: VideoUpdateOne, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      VideoQuery.encode(message.query, writer.uint32(10).fork()).join();
    }
    if (message.update !== undefined) {
      VideoUpdate.encode(message.update, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoUpdateOne {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoUpdateOne();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = VideoQuery.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = VideoUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoUpdateOne {
    return {
      query: isSet(object.query) ? VideoQuery.fromJSON(object.query) : undefined,
      update: isSet(object.update) ? VideoUpdate.fromJSON(object.update) : undefined,
    };
  },

  toJSON(message: VideoUpdateOne): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = VideoQuery.toJSON(message.query);
    }
    if (message.update !== undefined) {
      obj.update = VideoUpdate.toJSON(message.update);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoUpdateOne>, I>>(base?: I): VideoUpdateOne {
    return VideoUpdateOne.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoUpdateOne>, I>>(object: I): VideoUpdateOne {
    const message = createBaseVideoUpdateOne();
    message.query =
      object.query !== undefined && object.query !== null
        ? VideoQuery.fromPartial(object.query)
        : undefined;
    message.update =
      object.update !== undefined && object.update !== null
        ? VideoUpdate.fromPartial(object.update)
        : undefined;
    return message;
  },
};

function createBaseVideoUpdateById(): VideoUpdateById {
  return { id: '', update: undefined };
}

export const VideoUpdateById: MessageFns<VideoUpdateById> = {
  encode(message: VideoUpdateById, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== '') {
      writer.uint32(10).string(message.id);
    }
    if (message.update !== undefined) {
      VideoUpdate.encode(message.update, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VideoUpdateById {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideoUpdateById();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.update = VideoUpdate.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VideoUpdateById {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : '',
      update: isSet(object.update) ? VideoUpdate.fromJSON(object.update) : undefined,
    };
  },

  toJSON(message: VideoUpdateById): unknown {
    const obj: any = {};
    if (message.id !== '') {
      obj.id = message.id;
    }
    if (message.update !== undefined) {
      obj.update = VideoUpdate.toJSON(message.update);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VideoUpdateById>, I>>(base?: I): VideoUpdateById {
    return VideoUpdateById.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VideoUpdateById>, I>>(object: I): VideoUpdateById {
    const message = createBaseVideoUpdateById();
    message.id = object.id ?? '';
    message.update =
      object.update !== undefined && object.update !== null
        ? VideoUpdate.fromPartial(object.update)
        : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
