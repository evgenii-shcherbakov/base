{
  "name": "base",
  "private": true,
  "author": "Evgenii Shcherbakov <iipekolict@gmail.com>",
  "license": "MIT",
  "version": "0.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "dev:admin": "turbo run dev --filter=\"admin.*\"",
    "dev:admin.main": "turbo run dev --filter=admin.main",
    "dev:backend": "turbo run dev --filter=\"backend.*\"",
    "dev:backend.api-gateway": "turbo run dev --filter=backend.api-gateway",
    "dev:backend.main": "turbo run dev --filter=backend.main",
    "build": "turbo run build",
    "build:admin": "turbo run build --filter=\"admin.*\"",
    "build:admin.main": "turbo run build --filter=admin.main",
    "build:backend": "turbo run build --filter=\"backend.*\"",
    "build:backend.api-gateway": "turbo run build --filter=backend.api-gateway",
    "build:backend.public": "turbo run build --filter=backend.public",
    "prod": "turbo run prod",
    "prod:admin": "turbo run prod --filter=\"admin.*\"",
    "prod:admin.main": "turbo run prod --filter=admin.main",
    "prod:backend": "turbo run prod --filter=\"backend.*\"",
    "prod:backend.api-gateway": "turbo run prod --filter=backend.api-gateway",
    "prod:backend.public": "turbo run prod --filter=backend.public",
    "format": "turbo run format",
    "gen:package": "turbo gen package && yarn",
    "gen:backend-package": "turbo gen backend-package && yarn",
    "gen:backend-microservice": "turbo gen backend-microservice && yarn",
    "reset": "turbo run reset && rm -rf .turbo node_modules yarn.lock",
    "reset:admin": "turbo run reset --filter=\"admin.*\"",
    "reset:backend": "turbo run reset --filter=\"backend.*\"",
    "docker:local": "docker-compose -f docker-compose.local.yml -p base-local up --build",
    "docker": "docker-compose -f docker-compose.yml -p base up --build"
  },
  "dependencies": {
    "lodash": "4.17.21"
  },
  "devDependencies": {
    "@turbo/gen": "2.2.3",
    "@types/lodash": "4.14.202",
    "@types/node": "^20.3.1",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "change-case-all": "^2.1.0",
    "eslint": "^8.42.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.0",
    "prettier": "3.3.3",
    "tsup": "8.3.5",
    "turbo": "2.2.3",
    "typescript": "latest",
    "yarn": "1.22.22"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "admin/apps/*",
    "admin/packages/*",
    "backend/apps/*",
    "backend/packages/*",
    "frontend/apps/*",
    "frontend/packages/*",
    "packages/*",
    "packages/**/*",
    "libraries/*"
  ]
}
