syntax = 'proto3';

package main;

import "models/contact.proto";

service ContactService {
  rpc getMany (contact.ContactRequest) returns (contact.ContactList) {}
}

service MainContactService {
  rpc getMany (contact.ContactRequest) returns (contact.ContactList) {}
}
